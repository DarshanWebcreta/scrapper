services:
  - type: web
    name: bottle-scrapper-api
    runtime: docker
    dockerFilePath: backend/Dockerfile
    dockerContext: backend
    plan: free
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: GEMINI_API_KEY
        sync: false
