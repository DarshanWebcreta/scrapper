services:
  - type: web
    name: bottle-scrapper-backend
    env: docker
    dockerContext: backend
    dockerfilePath: backend/Dockerfile
    plan: free
    envVars:
      - key: DATABASE_URL
        value: sqlite:///./data/leads.db
      - key: GEMINI_API_KEY
        sync: false
